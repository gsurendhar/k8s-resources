apiVersion: v1
kind: Pod
metadata:
  name: env-configmapref
  labels:
    project: roboshop
    purpose: demo-nginx
spec:
  containers:
  - name: nginx
    image: nginx
    envForm:
    - configMapRef:            
      name: configmap-demo     # this is from 08-ConfigMap.yaml name
