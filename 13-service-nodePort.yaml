apiVersion: v1
kind: Service
metadata:
  name: service-np-demo
  labels:
    purpose: service-demo
spec:
  type: NodePort
  selector:     # pod labels must match with selector , based on this pod labels and selector, service can be attached to pod
    project: roboshop
    environment: dev
    purpose: service-np-demo
  ports: 
    - protocol: TCP
      port: 80   # Service Port
      targetPort: 80   # Container Port
---
apiVersion: v1
kind: Pod
metadata: 
  name: pod-service-np-demo
  labels:     # pod labels are more important
    project: roboshop
    environment: dev
    purpose: service-np-demo
spec:
  containers:
  - name: service-np-demo
    image: nginx
