apiVersion: v1
kind: Pod
metadata:
  name: env-Secret
  labels:
    project: roboshop
    purpose: demo-nginx
spec:
  containers:
  - name: nginx
    image: nginx
    envForm:
    - secretRef:            
      name: Pod-Secret     # this is from 10-secret.yaml name
